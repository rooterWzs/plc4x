/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
package org.apache.plc4x.java.omronfins.readwrite;

import static org.apache.plc4x.java.spi.codegen.fields.FieldReaderFactory.*;
import static org.apache.plc4x.java.spi.codegen.fields.FieldWriterFactory.*;
import static org.apache.plc4x.java.spi.codegen.io.DataReaderFactory.*;
import static org.apache.plc4x.java.spi.codegen.io.DataWriterFactory.*;
import static org.apache.plc4x.java.spi.generation.StaticHelper.*;

import java.time.*;
import java.util.*;
import org.apache.plc4x.java.api.exceptions.*;
import org.apache.plc4x.java.api.value.*;
import org.apache.plc4x.java.spi.codegen.*;
import org.apache.plc4x.java.spi.codegen.fields.*;
import org.apache.plc4x.java.spi.codegen.io.*;
import org.apache.plc4x.java.spi.generation.*;

// Code generated by code-generation. DO NOT EDIT.

public class FinsMessage implements Message {

  // Constant values.
  public static final Long PROTOCOLID = 0x46494E53L;
  public static final Long ERRORCODE = 0x00000000L;

  // Properties.
  protected final long command;
  protected final FinsMessageBody messageBody;

  public FinsMessage(long command, FinsMessageBody messageBody) {
    super();
    this.command = command;
    this.messageBody = messageBody;
  }

  public long getCommand() {
    return command;
  }

  public FinsMessageBody getMessageBody() {
    return messageBody;
  }

  public long getProtocolId() {
    return PROTOCOLID;
  }

  public long getErrorCode() {
    return ERRORCODE;
  }

  public void serialize(WriteBuffer writeBuffer) throws SerializationException {
    PositionAware positionAware = writeBuffer;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    writeBuffer.pushContext("FinsMessage");

    // Const Field (protocolId)
    writeConstField("protocolId", PROTOCOLID, writeUnsignedLong(writeBuffer, 32));

    // Implicit Field (length) (Used for parsing, but its value is not stored as it's implicitly
    // given by the objects content)
    long length = (long) ((getMessageBody().getLengthInBytes()) + (8L));
    writeImplicitField("length", length, writeUnsignedLong(writeBuffer, 32));

    // Simple Field (command)
    writeSimpleField("command", command, writeUnsignedLong(writeBuffer, 32));

    // Const Field (errorCode)
    writeConstField("errorCode", ERRORCODE, writeUnsignedLong(writeBuffer, 32));

    // Simple Field (messageBody)
    writeSimpleField("messageBody", messageBody, writeComplex(writeBuffer));

    writeBuffer.popContext("FinsMessage");
  }

  @Override
  public int getLengthInBytes() {
    return (int) Math.ceil((float) getLengthInBits() / 8.0);
  }

  @Override
  public int getLengthInBits() {
    int lengthInBits = 0;
    FinsMessage _value = this;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();

    // Const Field (protocolId)
    lengthInBits += 32;

    // Implicit Field (length)
    lengthInBits += 32;

    // Simple field (command)
    lengthInBits += 32;

    // Const Field (errorCode)
    lengthInBits += 32;

    // Simple field (messageBody)
    lengthInBits += messageBody.getLengthInBits();

    return lengthInBits;
  }

  public static FinsMessage staticParse(ReadBuffer readBuffer, Boolean response)
      throws ParseException {
    readBuffer.pullContext("FinsMessage");
    PositionAware positionAware = readBuffer;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();

    long protocolId =
        readConstField("protocolId", readUnsignedLong(readBuffer, 32), FinsMessage.PROTOCOLID);

    long length = readImplicitField("length", readUnsignedLong(readBuffer, 32));

    long command = readSimpleField("command", readUnsignedLong(readBuffer, 32));

    long errorCode =
        readConstField("errorCode", readUnsignedLong(readBuffer, 32), FinsMessage.ERRORCODE);

    FinsMessageBody messageBody =
        readSimpleField(
            "messageBody",
            readComplex(
                () ->
                    FinsMessageBody.staticParse(readBuffer, (long) (command), (boolean) (response)),
                readBuffer));

    readBuffer.closeContext("FinsMessage");
    // Create the instance
    FinsMessage _finsMessage;
    _finsMessage = new FinsMessage(command, messageBody);
    return _finsMessage;
  }

  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (!(o instanceof FinsMessage)) {
      return false;
    }
    FinsMessage that = (FinsMessage) o;
    return (getCommand() == that.getCommand())
        && (getMessageBody() == that.getMessageBody())
        && true;
  }

  @Override
  public int hashCode() {
    return Objects.hash(getCommand(), getMessageBody());
  }

  @Override
  public String toString() {
    WriteBufferBoxBased writeBufferBoxBased = new WriteBufferBoxBased(true, true);
    try {
      writeBufferBoxBased.writeSerializable(this);
    } catch (SerializationException e) {
      throw new RuntimeException(e);
    }
    return "\n" + writeBufferBoxBased.getBox().toString() + "\n";
  }
}
